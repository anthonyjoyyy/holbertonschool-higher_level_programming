#!/usr/bin/python3

# functiont that finds whether the value is divisble by 2 or not

def divisible_by_2(my_list=[]):

    newlist = []

    if my_list[] % 2 == 0:
        newlist.append(True)
    else:
        newlist.append(False)

    return newlist

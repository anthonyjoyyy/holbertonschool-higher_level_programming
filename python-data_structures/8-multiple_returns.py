#!/usr/bin/python3

# returning a tuple with the length of string and first element

def multiple_returns(sentence):

    if len(sentence) == 0
        return (0, None)

    tup = len(sentence), sentence[0]

    return tup

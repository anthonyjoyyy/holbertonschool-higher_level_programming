The ``1-my_list`` module
========================

Using ``MyList``
----------------

Importing the module and class:
    >>> MyList = __import__('1-my_list').MyList

Testing basic instantiation and appending:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> print(my_list)
    [1, 4, 2, 3, 5]

Testing print_sorted():
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

Verify original list wasn't modified:
    >>> print(my_list)
    [1, 4, 2, 3, 5]

Testing empty list:
    >>> empty_list = MyList()
    >>> empty_list.print_sorted()
    []

Testing with negative numbers:
    >>> neg_list = MyList([3, -1, 0, -10, 5])
    >>> neg_list.print_sorted()
    [-10, -1, 0, 3, 5]

Testing list with duplicates:
    >>> dup_list = MyList([2, 2, 1, 1])
    >>> dup_list.print_sorted()
    [1, 1, 2, 2]

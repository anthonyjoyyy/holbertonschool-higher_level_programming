#!/usr/bin/python3

# counting the number of keys in a dictionary

def number_keys(a_dictionary):
    number_of_keys = len(a_dictionary)  # len() can check the number of keys
    return number_of_keys
